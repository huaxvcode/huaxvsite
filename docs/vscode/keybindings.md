# keybingings

```json
// 将键绑定放在此文件中以覆盖默认值auto[]
[
    {
        "key": "ctrl+alt+p",
        "command": "codesnap.start"
    },
    {
        "key": "ctrl+alt+i",
        "command": "editor.action.insertSnippet",
        "args": { "name": "code" }
    }
]

```
